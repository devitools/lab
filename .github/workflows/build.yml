name: build floofy

on:
  push:
    tags:
      - 'v*'
    branches: [main]
    paths:
      - 'client/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: floofy-windows
            artifact_path: client/dist/floofy.exe
          - os: macos-latest
            artifact_name: floofy-macos
            artifact_path: client/floofy-macos.zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: install deps
        working-directory: client
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: build windows
        if: matrix.os == 'windows-latest'
        working-directory: client
        run: pyinstaller --onefile --windowed --name floofy --collect-all websockets --collect-all certifi app.py

      - name: build macos
        if: matrix.os == 'macos-latest'
        working-directory: client
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY_CONTENT: ${{ secrets.APPLE_API_KEY_CONTENT }}
        run: |
          pyinstaller --windowed --name floofy \
            --osx-bundle-identifier com.devitools.floofy \
            --collect-all websockets --collect-all certifi \
            app.py

          if [ -n "$APPLE_SIGNING_IDENTITY" ] && [ -n "$APPLE_CERTIFICATE" ]; then
            echo "::group::codesign"
            echo "$APPLE_CERTIFICATE" | base64 -d > /tmp/cert.p12
            KEYCHAIN_PWD=$(uuidgen)
            security create-keychain -p "$KEYCHAIN_PWD" build.keychain
            security set-keychain-settings -lut 21600 build.keychain
            security default-keychain -s build.keychain
            security unlock-keychain -p "$KEYCHAIN_PWD" build.keychain
            security import /tmp/cert.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
            security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PWD" build.keychain

            codesign --deep --force --verify --verbose --timestamp \
              --options runtime \
              --entitlements entitlements.plist \
              --sign "$APPLE_SIGNING_IDENTITY" \
              dist/floofy.app

            codesign --verify --deep --strict --verbose=2 dist/floofy.app
            echo "::endgroup::"

            if [ -n "$APPLE_API_KEY_CONTENT" ] && [ -n "$APPLE_API_KEY" ] && [ -n "$APPLE_API_ISSUER" ]; then
              echo "::group::notarize"
              mkdir -p "$HOME/private_keys"
              KEY_PATH="$HOME/private_keys/AuthKey_${APPLE_API_KEY}.p8"
              echo "$APPLE_API_KEY_CONTENT" > "$KEY_PATH"

              ditto -c -k --sequesterRsrc --keepParent dist/floofy.app /tmp/floofy-notarize.zip
              xcrun notarytool submit /tmp/floofy-notarize.zip \
                --key "$KEY_PATH" \
                --key-id "$APPLE_API_KEY" \
                --issuer "$APPLE_API_ISSUER" \
                --wait

              xcrun stapler staple dist/floofy.app
              echo "::endgroup::"
            else
              echo "::warning::Notarize secrets missing — assinado mas sem notarização (Gatekeeper ainda bloqueia)"
            fi
          else
            echo "::warning::Signing secrets missing — build não assinado (Gatekeeper bloqueia)"
          fi

          cd dist && ditto -c -k --sequesterRsrc --keepParent floofy.app ../floofy-macos.zip
          ls -la ../floofy-macos.zip

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          retention-days: 30

  release:
    needs: build
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: list artifacts
        run: ls -laR artifacts

      - name: rolling latest (main pushes)
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: floofy (rolling latest)
          files: |
            artifacts/floofy-windows/floofy.exe
            artifacts/floofy-macos/floofy-macos.zip
          make_latest: true
          body: |
            Build automático do último commit em `main`.

            - **Windows**: baixar `floofy.exe`
            - **macOS** (Apple Silicon): baixar `floofy-macos.zip`, descompactar, mover `floofy.app` pra Aplicativos. Na primeira abertura, click-direito → Abrir (Gatekeeper bloqueia binários sem assinatura).

      - name: tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/floofy-windows/floofy.exe
            artifacts/floofy-macos/floofy-macos.zip
          generate_release_notes: true
